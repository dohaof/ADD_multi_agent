spring:
  main:
    web-application-type: none
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      base-url: ${DASHSCOPE_BASE_URL:https://api.ppio.com/openai}
      chat:
        options:
          model: gpt-5.4

logging:
  level:
    com.group9: DEBUG
